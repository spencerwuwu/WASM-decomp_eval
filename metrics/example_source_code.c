#include <stdio.h>

int add(int a, int b) {
    char c = 'x';
    return a + b;
}

int main() {
    // printf() displays the string inside quotation
    int z = add(2, 4);
    printf("Hello, World!");
    return 0;
}
